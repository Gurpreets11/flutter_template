import 'package:core_package/core_package.dart';
import 'package:flutter/material.dart';

/// This app's brand theme configuration.
///
/// **This is the one file a new app built from this template needs to
/// change to look different** — nothing else in `features/` or `shell/`
/// hardcodes a color. Replace these values with the real brand palette
/// for whatever app you've scaffolded from this template.
const AppThemeConfig starterThemeConfig = AppThemeConfig(
  primary: Color(0xFF1565C0),
  secondary: Color(0xFF00897B),
  background: Color(0xFFF7F7F7),
  surface: Colors.white,
  error: Color(0xFFD32F2F),
);
